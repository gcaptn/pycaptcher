# pycaptcher

A quick screen capture tool I made to save online class slideshows (and for practice). Made with Tkinter, PyAutoGUI, and OpenCV.

## Running

```sh
pip install -r requirements.txt
main.py
```